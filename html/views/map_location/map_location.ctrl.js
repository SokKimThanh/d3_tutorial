var map_location_ctrl = function (map_location_service) {
    // map_location_service.notify("ecc"); 
    let div = d3.select("div.w3-container#map_location");
    div = div.append("div svg") 
    // let svg = d3.select("svg");
    // svg.append("image");
    // let image = d3.select("svg image")

}
